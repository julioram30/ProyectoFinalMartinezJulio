import { Routes, RouterModule } from '@angular/router';
import { FormAlumnosComponent } from './formAlumnos.component';

const routes: Routes = [
  { },
];

export const FormAlumnosRoutes = RouterModule.forChild(routes);
